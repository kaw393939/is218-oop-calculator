[pytest]
# Find tests here and make the repository's calculator package importable.
testpaths = tests
pythonpath = .
# Measure the application, show untested paths, and fail below 100% coverage.
addopts = --cov=calculator --cov-branch --cov-report=term-missing --cov-fail-under=100
